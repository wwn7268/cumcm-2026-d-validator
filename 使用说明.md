# D题学生附件验证器

用于检查学生提交的第二、三、四问附件，给出可行性判定、错误位置和时频占用矩阵。程序在本地读取文件，不修改学生原件，不上传数据。

当前版本 **v1.1.0**：增加第四问验证、改间隔统计和示例，保留连续验证时的绘图刷新修复。更新后须关闭旧窗口，再启动程序；窗口标题应显示 v1.1.0。

## 打开与使用

1. 双击 **启动验证器.cmd**。
2. 选择问题2、问题3或问题4，选择学生提交的 Excel 附件（`.xlsx` / `.xlsm`，也支持 CSV）。
3. 验证第三问时，另选学生采用的第二问附件。第二、四问直接以原始150台装备为基准，不需要第二问附件。
4. 核对时间上限 H，点击 **开始验证**。默认 H=643，沿用原计划最晚结束时刻；这一时间窗是建模口径，不是题目明确给出的截止时间，应与学生论文假设一致。
5. 查看“判定与统计”“问题与冲突”“时频矩阵”。双击冲突记录可定位到对应图域；矩阵工具栏可放大、平移、恢复全图，鼠标悬停显示占用装备及第几次使用。
6. 点击 **导出报告**，在所选目录中新建独立结果文件夹。文件夹包含 PNG 图、Excel 明细、CSV 占用计数矩阵、JSON 完整结果和文字摘要。

可先点击“载入可行示例”了解流程。`examples/` 中另有故意出错的附件，便于课堂演示。

## 附件格式

采用题目官方表头。Excel 多工作表时可手动选择；自动识别不能确定唯一数据表时，需选定工作表。

第二问四列：

| 用频装备编号 | 调整后频段区间 | 调整后时间区间 | 是否撤销用频计划 |
|---|---|---|---|
| A001 | [75,85) | | |
| B002 | | | 是 |

仅列出调整或撤销的装备，未出现的装备保持原计划。每个编号至多一行。调整频段、调整首次时间、撤销三种操作中每个装备至多选一种。区间采用 `[下界,上界)`，单位分别为 Δf、Δt，端点须为整数。不能把逗号分隔的区间拆成两列。

第三问三列：

| 新增用频装备序号 | 调整后频段区间 | 调整后时间区间 |
|---|---|---|
| 1 | [0,3) | [0,2) |

第三问表只记录新增C类装备。频宽3、单次时长2、间隔8、使用12次，均由题目设定，不能改动。表中时间区间是首次使用区间；程序会展开全部12次使用。上述行仅用于说明格式，不保证它与某个第二问方案不冲突。

第四问五列：

| 用频装备编号 | 调整后频段范围 | 调整后时间区间 | 调整后间隔时长 | 是否撤销用频计划 |
|---|---|---|---|---|
| C001 | | | 7 | |
| B002 | | | | 是 |

“调整后间隔时长”填写**新的间隔值**，不是变化量。例如原间隔8，改成7，则填7；改成0应明确填数字0。空白表示沿用原值。只列调整或撤销的装备，其余保持原始计划。上述两行用于说明格式，不代表一份完整的无冲突方案。

程序拒绝公式单元格，避免把未计算或过期的缓存值当作学生数据；请提交保存为数值/文字的附件。不支持旧版二进制 `.xls`，可先在表格软件中另存为 `.xlsx`。

## 判定规则

- 原始数据固定使用随程序提供的 `data/original_plans.json`，由题目附件1整理：A类20个、B类40个、C类90个。
- 第二问：频段整体平移幅度不超过10Δf，首次时间整体平移幅度不超过5Δt；保持频宽、时长、间隔、使用次数；调整一个参数或撤销。
- 第三问：固定对应第二问的可行执行方案。新增C类不受第二问平移幅度限制，但所有使用均须落在频率 `[0,100)`、时间 `[0,H)` 内，且不能与已有或新增装备冲突。
- 第四问：沿用第二问的调频、调时规则，仅C类额外允许改变间隔。新旧间隔之差的绝对值不超过10Δt，新间隔非负，即本题C类的新间隔取整数0～18。A、B类间隔不能改变。调频、调时、改间隔至多一项，或者撤销；频宽、单次时长、使用次数保持不变。
- 第四问仅改间隔时，首次开始不变，后续使用按“时长+新间隔”逐次展开。后续时刻的累积变化不受调时±5限制，但每次占用必须满足资源边界且无冲突。第四问从原始150台重新构造计划，不加入第三问新增装备。
- 间隔指本次使用结束至下次开始的空闲时长，重复周期为“单次时长+间隔”。
- 时间与频率均采用半开区间；只接触边界不算冲突。若两个装备在两个维度均有正长度交叠，则发生冲突。
- 任意一项规则违反、输入未成功解析、基础方案不可行，均不能获得“可行”判定。
- “可行”只说明满足这一口径下的约束，不证明撤销数、调整数、调整幅度或新增数量最优。第二、四问的九项指标按“总撤销、A撤销、B撤销、总调整、A调整、B调整、总幅度、A幅度、B幅度”顺序列出，幅度按基本单位等权相加。第四问仅改间隔的装备计1次调整，其幅度计新旧间隔之差的绝对值，不按多次使用的累积位移计数。

## 矩阵与利用率

横轴是时间，纵轴是频率。每一个格表示 Δt×Δf 的时频单元。上图按 A、B、原有C、新增C 着色；红色表示占用数至少2。下图显示每格占用次数。冲突表按实际交叠事件列出，可能有同一装备对多次相遇。

面积利用率 = 实际占用区域的并集面积 ÷ (100H)。空闲间隔不计入占用面积。有冲突时，面积并集仍只计一次；同时另列各次矩形面积之和及冲突格数，不能用重复计数掩盖冲突。越界或格式错误的方案，图中只能显示可解析且位于窗口内的部分，不能据图认定可行。

## 命令行

在本目录运行：

```powershell
python -X utf8 validate.py 2 "examples/result2_可行示例.xlsx"
python -X utf8 validate.py 3 "examples/result3_可行示例.xlsx" --base-q2 "examples/result2_可行示例.xlsx"
python -X utf8 validate.py 4 "examples/result4_可行示例.xlsx"
```

可用 `--horizon 643`、`--sheet Sheet1`、`--base-sheet Sheet1`、`--output 输出目录`。退出码0表示可行，1表示不可行，2表示输入错误或校验未完成。每次报告写到新的子目录，不覆盖原始附件。

## 运行环境

Python 3.10或以上，需有 Tkinter、openpyxl、numpy、matplotlib。下载源码ZIP后先完整解压，并准备上述组件；可在本目录运行 `python -m pip install -r requirements.txt` 安装Python依赖，Tkinter需由Python安装环境提供。本程序不会自动安装、升级软件，也不能直接在GitHub网页中运行。Windows图形界面已测试，其他系统需自行提供Tkinter和可用中文字体。

主要文件：`app.py` 图形界面，`engine.py` 独立验证规则，`visuals.py` 绘图，`reporting.py` 报告输出，`validate.py` 命令行入口。原始数据及示例均随程序提供，不依赖原工程路径。

## 已完成的检查

v1.1.0 已通过54项规则测试和3项界面测试。第四问示例为此前四问完整稿的方案：撤销4台、调整140台，其中调频66台、调时33台、改间隔41台；占用面积16176，冲突0。矩阵、CSV和Excel报告中的改间隔数量及幅度已核对。

已有检查覆盖官方格式示例、重复编号、非法区间、单参数限制、完整重复周期、端点接触、基础方案失败传播，以及附件未列出装备在自定义时间窗下的边界检查。随程序附带的150台原始装备已经逐条核对附件1。规则测试见 `tests/test_engine.py`，连续绘图与工具栏测试见 `tests/test_gui_refresh.py`，第四问专项测试见 `tests/test_q4.py`。

在部分字体环境中，绘图日志可能出现非致命的粗体字重回退提示；开发环境中的中文标题及图例显示正常。
